(() => {
  "use strict";
  const lastUpdatedEl = document.getElementById("last-updated");
  const statusEl = document.getElementById("status");
  lastUpdatedEl.textContent = "Last updated: — (wired in Phase 3)";
  statusEl.textContent = "UI mock loaded.";
})();